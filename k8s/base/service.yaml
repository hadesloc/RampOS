apiVersion: v1
kind: Service
metadata:
  name: rampos-server
  namespace: rampos
spec:
  selector:
    app: rampos-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
