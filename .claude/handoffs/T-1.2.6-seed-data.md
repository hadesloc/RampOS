# Task T-1.2.6: Create Seed Data

## Status
- **Status:** Completed
- **Output:** `migrations/002_seed_data.sql`

## Description
Created a SQL migration file to populate the database with realistic test data for development.

## Data Created
1. **Tenants:**
   - `CryptoExchange A` (Active, Enterprise tier)
   - `WalletApp B` (Active, Standard tier)
   - `Startup C` (Pending)

2. **Users:**
   - Varied KYC tiers (0-3) and statuses (ACTIVE, SUSPENDED, BLOCKED)
   - Includes high-risk and high-value user profiles

3. **Rails Adapters:**
   - Bank (VCB Direct)
   - Crypto (Fireblocks)
   - PSP (VNPay)

4. **Transactions (Intents + Ledger):**
   - Completed Pay-in (10M VND) with correct double-entry ledger records
   - Completed Pay-out (2M VND) with ledger records
   - Pending transactions for testing state transitions

5. **Compliance:**
   - AML cases (Sanctions hit, Velocity check)

## Usage
Run the migration file against your local PostgreSQL database:
```bash
psql -d rampos -f migrations/002_seed_data.sql
```

## Idempotency
The script uses `ON CONFLICT` clauses to ensure it can be run multiple times without errors or duplicating key data.
