# Task T-5.1.4: Implement tier checks in intent flows

## Status
Completed

## Changes
- **ramp-compliance**: Updated `KycTier` limits in `types.rs`.
- **ramp-core**:
  - Added `get_daily_payin_amount` and `get_daily_payout_amount` to `IntentRepository`.
  - Implemented tier checks and daily cumulative limit checks in `PayinService` and `PayoutService`.
  - Updated `CreatePayinResponse` and `CreatePayoutResponse` to include `daily_limit` and `daily_remaining`.
  - Updated `MockIntentRepository` for testing.
- **ramp-api**:
  - Mapped `UserLimitExceeded` error to HTTP 403 Forbidden.
  - Updated `create_payin` and `create_payout` handlers to include `X-User-Daily-Limit` and `X-User-Daily-Remaining` headers.

## Verification
- Verified code changes against requirements.
- Updated mock repository to support new repository methods.
- Verified that `UserLimitExceeded` error is correctly handled in API layer.
- Verified that response headers are added in API layer.

## Files Modified
- `crates/ramp-compliance/src/types.rs`
- `crates/ramp-core/src/repository/intent.rs`
- `crates/ramp-core/src/service/payin.rs`
- `crates/ramp-core/src/service/payout.rs`
- `crates/ramp-api/src/error.rs`
- `crates/ramp-api/src/handlers/payin.rs`
- `crates/ramp-api/src/handlers/payout.rs`
- `crates/ramp-core/src/test_utils.rs`
