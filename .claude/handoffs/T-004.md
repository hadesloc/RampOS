# T-004 - Enable Redis Auth

## Status
Completed

## Changes
1.  **Rust Configuration (`crates/ramp-core/src/config/mod.rs`)**:
    -   Updated `RedisConfig::default()` to use `redis://:dev_redis_pass@localhost:6379`.
    -   Ensures local development matches `docker-compose.yml` configuration (which sets `requirepass`).
    -   Added unit test `test_redis_default_config` to prevent regression.

2.  **Example Configuration (`config.toml`)**:
    -   Updated `redis.url` to include the password `dev_redis_pass`.

3.  **CI Configuration (`.github/workflows/ci.yaml`)**:
    -   Updated environment variable `REDIS_URL` to `RAMPOS__REDIS__URL` to correctly override the application configuration.
    -   Kept the value as `redis://localhost:6379` (no password) to match the CI Redis service configuration.

4.  **Test Script (`scripts/run-full-suite.sh`)**:
    -   Updated `RAMPOS__REDIS__URL` to include the password, ensuring integration tests can authenticate against the dockerized Redis.

## Verification
-   **Unit Tests**: Ran `cargo test -p ramp-core` -> All passed (including the new config test).
-   **Infrastructure**: Verified `docker-compose.yml` and `k8s/base/redis-statefulset.yaml` already enforce password authentication (`--requirepass`).
-   **CI**: explicit `RAMPOS__REDIS__URL` override ensures CI passes.

## Next Steps
-   None. Redis authentication is now enabled by default in the application configuration.
