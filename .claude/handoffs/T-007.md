# Task T-007: Fix .expect() in ramp-core

## Status
Completed

## Changes
- Replaced `unwrap()` with `ok_or_else()` in `crates/ramp-core/src/workflows/compensation.rs` in `CompensatedStep::execute`.
- Fixed build errors in `crates/ramp-adapter/src/factory.rs` (prerequisite for running tests):
  - Removed invalid `?` operator on `VietQRAdapter::new` and `NapasAdapter::new`.
  - Added `.expect()` to `AdapterFactory::default()` to handle `Result` from `new()`.

## Verification
- `cargo check -p ramp-core` passes.
- `cargo test -p ramp-core workflows` passes (32 tests).
- Verified no other `unwrap()` or `expect()` calls exist in `ramp-core` production code (all remaining are in `tests` modules or using safe `unwrap_or`).

## Notes
- Codebase is now safer from panics in workflow compensation logic.
