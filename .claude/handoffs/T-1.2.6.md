# Task T-1.2.6: Add Seed Data for Development/Testing

## Status
- **Status:** Completed
- **Output:** `migrations/999_seed_data.sql` (supplements `migrations/002_seed_data.sql`)

## Description
Implemented comprehensive seed data generation script to populate the development database with realistic scenarios. This extends the initial seed data to meet the requirement of "5+ users per tenant" and covers complex flows like trading and edge cases.

## Implementation Details

### 1. Data Coverage
- **Tenants:** Verified 3 tenants (CryptoExchange, WalletApp, Startup).
- **Users:** Added users to ensure >5 users per tenant with mixed KYC tiers (0, 1, 2, 3) and statuses (ACTIVE, BLOCKED).
- **Intents:**
  - Added `TRADE_EXECUTED` scenarios (VND -> USDT).
  - Added edge cases: `EXPIRED` payins, `CANCELLED` payouts.
- **Ledger:**
  - Generated double-entry ledger records for all new intents.
  - Verified `ASSET` vs `LIABILITY` consistency.
- **Webhooks:**
  - Configured webhook URLs.
  - Added sample `webhook_events` (DELIVERED, FAILED, PENDING).

### 2. Files
- `migrations/999_seed_data.sql`: Main extension script. Uses `ON CONFLICT` to be idempotent.

## Usage
Run migrations in order:
```bash
psql -d rampos -f migrations/001_initial_schema.sql
psql -d rampos -f migrations/002_seed_data.sql
psql -d rampos -f migrations/999_seed_data.sql
```

## Verification Scenarios
1. **User Diversity:** Query users per tenant to verify count >= 5.
2. **Trade Logic:** Check ledger entries for `intent_trade_a5_1` to see VND debit and USDT credit.
3. **Webhook Testing:** Check `webhook_events` table for sample payloads.
