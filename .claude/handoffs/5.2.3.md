# Task 5.2.3: Add KYC verification workflow

## Implementation Details
- Created `crates/ramp-compliance/src/kyc/workflow.rs` implementing `KycVerificationWorkflow`.
- Updated `crates/ramp-compliance/src/kyc/mod.rs` to export `workflow` and add `Serialize`/`Deserialize` to `KycDocument`.
- Implemented workflow logic:
  - Document validation
  - Provider submission
  - Signal handling (ProviderCallback, ManualApprove, ManualReject)
  - Case creation for failures/manual review
  - Tier update on success

## Files Created/Modified
- `crates/ramp-compliance/src/kyc/workflow.rs` (New)
- `crates/ramp-compliance/src/kyc/mod.rs` (Modified)

## Testing
- Added unit tests in `crates/ramp-compliance/src/kyc/workflow.rs` covering:
  - Happy path (Approved)
  - Manual review path (Pending -> ManualApprove)
- Verified logic handles all requirements including signal waiting and case creation.

## Notes
- `KycDocument` now implements `Serialize` and `Deserialize`.
- Workflow input uses `String` for IDs to ensure serialization compatibility.
