# Task 4.4.3: Phase 1 Staging Deployment Preparation

## Completed Items
- **Staging Overlay**: Created `k8s/overlays/staging/kustomization.yaml` with:
  - 2 replicas for high availability but lower cost
  - Reduced CPU/Memory limits
  - Staging environment variables (RUST_LOG=info, ENVIRONMENT=staging)
  - Ingress host `staging-api.rampos.io`
- **CI/CD Pipeline**: Created `.github/workflows/deploy-staging.yaml`:
  - Triggers on push to `staging` branch
  - Builds and pushes Docker image with `staging` tag
  - Updates kustomization with new image SHA
  - Deploys using `kubectl apply -k`
  - Runs basic smoke test (curl health endpoint)
- **Documentation**: Updated `docs/DEPLOY.md`:
  - Added staging deployment section
  - Documented manual deployment steps
  - Added environments table

## Artifacts
- `k8s/overlays/staging/kustomization.yaml`
- `.github/workflows/deploy-staging.yaml`
- `docs/DEPLOY.md`

## Next Steps
1. Create `staging` branch in git.
2. Add `KUBECONFIG_STAGING` secret to GitHub repository.
3. Configure DNS for `staging-api.rampos.io`.
4. Perform first deployment test.
