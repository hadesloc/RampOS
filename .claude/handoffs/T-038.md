# Task T-038: Refactor Intents Page to use DataTable Component

## Status: COMPLETED

## Summary
Refactored the Intents page (`frontend/src/app/(admin)/intents/page.tsx`) to use the reusable `DataTable` component instead of raw Table components.

## Changes Made

### File Modified
- `frontend/src/app/(admin)/intents/page.tsx`

### Implementation Details

1. **Replaced raw Table with DataTable component**
   - Imported `DataTable` from `@/components/dashboard/data-table`
   - Imported `ColumnDef` from `@tanstack/react-table`
   - Imported `ArrowUpDown` icon for sort indicators

2. **Created sortable column definitions**
   - ID column: sortable with truncated display and link to detail page
   - Type column: sortable with colored Badge for PAYIN/PAYOUT/TRADE
   - State column: sortable with StatusBadge component (preserved)
   - Amount column: sortable with custom numeric sorting function
   - Reference column: non-sortable (simple text display)
   - CreatedAt column: sortable with custom date sorting function

3. **Optimized filtering with useMemo**
   - Changed `filteredIntents` to use `useMemo` for better performance

4. **Preserved existing functionality**
   - All mock data kept intact
   - Filter dropdowns (type, state) working
   - Search input for ID and reference code
   - StatusBadge component for state column
   - Type color badges (blue/purple/orange)
   - Amount formatting with VND currency
   - Date formatting in Vietnamese locale

5. **Added pagination**
   - DataTable includes built-in pagination controls (Previous/Next)

## Testing
- Build passed successfully: `npm run build`
- All pages generated without errors
- TypeScript compilation successful

## Files Changed
| File | Action |
|------|--------|
| `frontend/src/app/(admin)/intents/page.tsx` | Modified |

## Dependencies
- `@tanstack/react-table` (already installed)
- `@/components/dashboard/data-table` (existing component)

## Completed By
Worker Agent - 2026-02-06
