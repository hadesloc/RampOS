# Task T-10.9: Enterprise Onboarding Wizard

## Status
- [x] Create `apps/admin/app/(admin)/onboarding/page.tsx`
- [x] Implement multi-step form with `react-hook-form` and `zod`
- [x] Step 1: Company Info
- [x] Step 2: Branding (Logo upload mock, Color picker)
- [x] Step 3: API Configuration
- [x] Step 4: Review & Submit
- [x] Use Shadcn components for UI

## Implementation Details
- Used `zod` schemas for each step and combined them for the final submission.
- Managed step state with `useState`.
- Used `trigger` from `react-hook-form` to validate current step fields before proceeding.
- Mocked logo upload with a clickable area.
- Included validation for all required fields including hex color code and URL.
- Added visual step indicator.

## Verification
- Can navigate through steps 1-4.
- Validation prevents moving to next step if fields are invalid.
- Review step shows summarized data.
- Final submit triggers console log and alert (mock submission).

## Next Steps
- Connect `onSubmit` to actual backend API endpoint.
- Implement real file upload for logo.
- Add more robust error handling and loading states.
